export default interface boardAction {
    boardIdx: number,
    buyPrice: number,
    gender: number,
    liveEndTime: Date,
    liveStartTime: Date,
    liveState: number,
    pattern: string,
    unit: number,
    size: string,
    startPrice: string,
    state: number,
    streamKey: string,
}